from typing import List


def mean_calculator(dataset: List[float]):
    dataset_length = len(dataset)
    total = sum(dataset)
    mean: float = total / dataset_length
    return mean


def median_calculator(dataset: List[float]):
    median = []
    return median


def mode_calculator(dataset: List[float]):
    mode = []
    return mode


def variance_calculator(dataset: List[float]):
    variance = []
    return variance


def standard_deviation_calculator(dataset: List[float]):
    standard_deviation = []
    return standard_deviation
